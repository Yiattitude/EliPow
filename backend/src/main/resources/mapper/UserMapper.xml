<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elipow.backend.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.elipow.backend.entity.User">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="role" property="role"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, email, password, nickname, avatar_url, role, enabled, created_at, updated_at
    </sql>

    <insert id="register" parameterType="com.elipow.backend.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (email, password, nickname, role)
        VALUES (#{email}, #{password}, #{nickname}, COALESCE(#{role}, 'STUDENT'))
    </insert>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM `user`
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM `user`
        WHERE id = #{id}
    </select>

</mapper>
